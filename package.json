{
	"private": true,
	"workspaces": [
		"ui-library",
		"frontend"
	],
	"scripts": {
		"build:tokens": "cd ui-library && yarn run build:tokens",
		"storybook": "cd ui-library && yarn run start"
	},
	"dependencies": {
		"yarn": "^1.22.18"
	}
}
