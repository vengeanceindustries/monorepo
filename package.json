{
	"private": true,
	"workspaces": [
		"component-library",
		"design-tokens",
		"frontend"
	],
	"scripts": {
		"build:tokens": "cd design-tokens && yarn run build:tokens",
		"storybook": "cd component-library && yarn run start"
	}
}
