{
	"private": true,
	"workspaces": [
		"component-library",
		"design-tokens",
		"frontend"
	],
	"scripts": {
		"tokens": "cd design-tokens && yarn run generate",
		"storybook": "cd component-library && yarn run start"
	}
}
